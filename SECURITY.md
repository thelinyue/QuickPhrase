# QuickPhrase 安全政策

## 支持版本

| 版本 | 状态 |
| --- | --- |
| `0.0.1` 系列 | 准备发布并接受安全修复 |
| 更早的开发快照 | 不提供独立安全维护承诺 |

## 报告安全问题

安全问题不得提交公开 Issue，也不要在公开讨论中披露利用方式、用户数据或可复现秘密。

请使用 GitHub **Private Vulnerability Reporting**：

[私密报告 QuickPhrase 安全问题](https://github.com/thelinyue/QuickPhrase/security/advisories/new)

报告建议包含：

- 受影响版本或 commit。
- Windows 和目标应用环境。
- 最小复现步骤。
- 实际结果和预期安全结果。
- 是否涉及错窗口投递、无授权发送、重复发送、剪贴板、令牌或用户数据。
- 可安全共享的日志、TraceId 和错误码。

不得提交真实话术正文、联系人、聊天记录、密码、Token 或客户资料。

## 响应目标

维护者力争：

- 3 个工作日内确认收到报告。
- 7 个工作日内给出初步影响评估或补充信息请求。
- 高危问题优先停止受影响 Release 的继续分发，并准备修复版本和安全公告。

这些是响应目标，不构成服务等级协议。

## 重点安全边界

QuickPhrase 的最高原则是宁可不能发送，也不能发错窗口、发错内容或重复发送。重点范围包括：

- 目标变化后仍投递。
- 无用户授权的发送或后台发送。
- 失败后自动重试导致重复发送。
- 剪贴板恢复覆盖用户的新复制内容。
- 企业同步密码或设备 Token 泄露。
- 企业同步认证、只读边界或缓存隔离绕过。
- SQLite 数据损坏、迁移失败或越权读取。
- GitHub Actions、SignPath workflow 或 Artifact Configuration 被篡改。
- Release tag、安装器、哈希或 manifest 被替换。

## 修复和披露

- 修复使用新的 commit 和版本，不移动或强推已有正式 tag。
- 受影响资产会从 Release 中停止分发或明确撤销，并发布安全说明。
- 新资产必须重新构建、测试、签名和验证 SHA-256。
- 披露时间会与报告者协调，但可能因已被公开利用而提前。

## 非安全问题

普通缺陷、功能请求和界面问题请使用公开 GitHub Issues。
