# QuickPhrase 隐私政策

最后更新：2026-08-20

QuickPhrase（闪语）是一款面向 Windows 11 的开源本地快捷话术工具。本政策说明正式桌面应用实际处理的数据、可选企业同步行为和用户控制边界。

## 默认本地模式

默认情况下，QuickPhrase 不连接 QuickPhrase 官方云服务。话术、分类、设置、搜索历史、企业缓存和诊断日志保存在当前 Windows 用户目录：

```text
%LOCALAPPDATA%\QuickPhrase
```

默认本地模式不会上传用户话术、联系人、聊天内容、窗口标题、输入框内容或剪贴板。QuickPhrase 不包含广告或第三方分析 SDK，也不使用第三方行为分析或广告跟踪。

## 本地处理的数据

QuickPhrase 可以在本机处理：

- 用户创建的话术标题和正文。
- 分类、颜色、排序和使用次数。
- 本机搜索历史。
- Launcher、快捷键和启动设置。
- 投递目标的脱敏运行时身份和能力状态。
- 错误码、TraceId、阶段和耗时。

日志禁止记录话术正文、聊天内容、联系人、客户资料、输入框文字、密码、设备 Token 或剪贴板内容。

## 企业同步

只有用户在设置中主动配置并连接企业内网中的闪语中心时，QuickPhrase 才会启用企业同步。

连接和同步可能向用户指定的 Hub 地址传输：

- Hub 地址。
- 企业账号。
- 连接时输入的密码。
- 设备名称。
- QuickPhrase 客户端版本。
- 设备 Token。
- 同步游标、缓存代次和企业发布号。
- 企业分类。
- 企业话术标题及正文。

密码不持久化，只用于当前连接请求。认证成功后的设备 Token 使用 Windows DPAPI CurrentUser 保护后保存在当前用户目录。Hub 地址、账号、设备 ID、同步状态和发布号保存在本地数据库；企业分类和企业话术作为只读缓存保存在本地 SQLite。

当前桌面客户端只接受用户明确填写的 `http://` 内网 Hub 地址，不自动连接公共互联网服务。企业部署者负责提供可信内网、访问控制和传输安全边界。

## Windows 目标和剪贴板

用户触发插入时，QuickPhrase 会在本机检测当前前台窗口和输入焦点，并使用受保护的剪贴板事务执行粘贴。目标变化、权限不一致或焦点无法确认时会停止动作。

QuickPhrase 不读取企业微信聊天正文，也不会把剪贴板内容上传到任何服务。

## 数据保留和删除

- 卸载程序默认保留 `%LOCALAPPDATA%\QuickPhrase` 中的数据、备份和日志，便于重新安装后恢复。
- 用户可以在设置中清除搜索历史或企业缓存。
- 断开企业同步时会删除本机设备 Token；用户可以选择是否保留企业只读缓存。
- 删除本地数据目录会永久删除本机话术、设置、缓存、日志和备份，请在操作前自行备份。

## 第三方组件

QuickPhrase 使用开源 .NET 依赖和 Windows 系统能力。依赖许可证以仓库源码和构建清单为准。正式发布不包含 WebView2 Runtime、广告 SDK 或第三方分析 SDK。

## 政策变更

隐私行为发生变化时，本政策会随源码提交更新。公开 Release 对应的政策版本以该 Release tag 中的文件为准。

## 联系与安全问题

一般问题可以使用 GitHub Issues。安全问题不得提交公开 Issue，请按照 [SECURITY.md](SECURITY.md) 使用 GitHub Private Vulnerability Reporting。
