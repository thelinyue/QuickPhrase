<!-- 自定义标题栏：左侧品牌区（属于窗口 caption，可拖动/双击最大化/Snap），右侧系统按钮可点击 -->
<UserControl x:Class="QuickPhrase.Desktop.TitleBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:QuickPhrase.Desktop"
             xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework">

    <Grid Height="32" Background="{StaticResource AppBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!-- 拖动 / 双击最大化 / Snap 区域（非命中可见，走原生 caption）。
             仅显示当前页标题（话术库）。 -->
        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="14,0,0,0">
            <TextBlock Text="{Binding PageTitle, RelativeSource={RelativeSource AncestorType=local:TitleBar}}"
                       Style="{StaticResource TextListTitle}" FontSize="14" Margin="0"
                       Foreground="{StaticResource TextBrush}" />
        </StackPanel>

        <!-- 系统按钮（命中可见，可点击） -->
        <StackPanel Grid.Column="1" Orientation="Horizontal"
                    shell:WindowChrome.IsHitTestVisibleInChrome="True">
            <Button x:Name="MinButton" Style="{StaticResource GhostButton}"
                    Width="46" Height="32" Click="MinButton_Click" Content="—" />
            <Button x:Name="MaxButton" Style="{StaticResource GhostButton}"
                    Width="46" Height="32" Click="MaxButton_Click" Content="▢" />
            <Button x:Name="CloseButton" Style="{StaticResource GhostButton}"
                    Width="46" Height="32" Click="CloseButton_Click" Content="✕" />
        </StackPanel>
    </Grid>
</UserControl>
