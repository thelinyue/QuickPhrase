<Window x:Class="QuickPhrase.Desktop.PhraseMoveDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="{StaticResource Size.Dialog.PhraseMove.Width}" Height="{StaticResource Size.Dialog.PhraseMove.Height}"
        Style="{StaticResource Style.Dialog.Window}" Title="移动到分类"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="{StaticResource Thickness.Dialog}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="选择目标分类" Style="{StaticResource Style.Text.Label}" Margin="{StaticResource Thickness.Dialog.Label}" />
        <ComboBox x:Name="CategoryCombo" Grid.Row="1" Style="{StaticResource Style.Select.Default}"
                  ItemsSource="{Binding Categories}" DisplayMemberPath="Name" SelectedValuePath="Id" />
        <TextBlock x:Name="ErrorText" Grid.Row="2" Foreground="{DynamicResource Brush.Status.Error}"
                   Style="{StaticResource Style.Text.Caption}" VerticalAlignment="Center" />
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="{StaticResource Thickness.Gap.Stack.Before.MD}">
            <Button Style="{StaticResource Style.Button.Ghost}" Content="取消" Click="Cancel_Click" Margin="{StaticResource Thickness.Gap.Inline.SM}" />
            <Button Style="{StaticResource Style.Button.Primary}" Content="移动" Click="Ok_Click" />
        </StackPanel>
    </Grid>
</Window>
