<!-- 主窗口：纯 WPF 原生外壳。顶部自定义标题栏（32px = WindowChrome CaptionHeight），左侧导航面板，右侧内容区。 -->
<Window x:Class="QuickPhrase.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:QuickPhrase.Desktop"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="闪语"
        Icon="pack://application:,,,/QuickPhrase;component/Assets/quickphrase.ico"
        Width="1200" Height="760" MinWidth="900" MinHeight="560"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource AppBackgroundBrush}"
        FontFamily="{StaticResource UiFontFamily}">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="32"
                            ResizeBorderThickness="6"
                            GlassFrameThickness="0"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <local:TitleBar x:Name="TitleBarControl" Grid.Row="0" />
        <ContentControl x:Name="ContentRegion" Grid.Row="1" Background="{StaticResource SurfaceBrush}" />
    </Grid>
</Window>
